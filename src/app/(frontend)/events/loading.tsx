/**
 * Events Page Loading Skeleton
 * Thin wrapper over the shared timeline skeleton
 */

import { TimelineListSkeleton } from '@/components/ui/timeline-list-skeleton'

export default function Loading() {
  return <TimelineListSkeleton />
}
