/**
 * Food Page Loading Skeleton
 * Thin wrapper over the shared timeline skeleton (with vendor-logo slots)
 */

import { TimelineListSkeleton } from '@/components/ui/timeline-list-skeleton'

export default function Loading() {
  return <TimelineListSkeleton showLogo />
}
